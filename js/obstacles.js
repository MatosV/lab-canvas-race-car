class Game {
  constructor(game){
    this.game = game;

    this.placeRandom();
  }

  //RANDOM OBSTACLES
  placeRandom() {
    let x = Math.floor(Math.random() * );

    this.x = x;
    //this.y = y;
  }

}